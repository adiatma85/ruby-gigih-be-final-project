<%= form_with(model: order) do |form| %>
  <% if order.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
        <% order.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="card-body">
    <div class="form-group">
      <%= form.label :email %>
      <%= form.email_field :email, class: "form-control" %>
    </div>

    <div class="form-group">
    <%= form.label :menus %>

    <div class="row my-2">
      <div class="col-9">
        <select class="custom-select rounded-0" name="menus[]" id="menus">
          <option value="" selected>Open this select menu</option>
          <% @menus.each do |menu| %>
            <option value="<%= menu.id.to_s + "_" + menu.price.to_s %>"><%= menu.name %></option>
          <% end %>
        </select>
      </div>
      <div class="col-3">
        <input type="number" name="menu_qty[]" id="quantity" class="form-control">
      </div>
    </div>

    <div class="row my-2">
      <div class="col-9">
        <select class="custom-select rounded-0" name="menus[]" id="menus">
          <option value="" selected>Open this select menu</option>
          <% @menus.each do |menu| %>
            <option value="<%= menu.id.to_s + "_" + menu.price.to_s %>"><%= menu.name %></option>
          <% end %>
        </select>
      </div>
      <div class="col-3">
        <input type="number" name="menu_qty[]" id="quantity" class="form-control">
      </div>
    </div>

    <div class="row my-2">
      <div class="col-9">
        <select class="custom-select rounded-0" name="menus[]" id="menus">
          <option value="" selected>Open this select menu</option>
          <% @menus.each do |menu| %>
            <option value="<%= menu.id.to_s + "_" + menu.price.to_s %>"><%= menu.name %></option>
          <% end %>
        </select>
      </div>
      <div class="col-3">
        <input type="number" name="menu_qty[]" id="quantity" class="form-control">
      </div>
    </div>
  </div>
  </div>

  <%# Nanti di sini ada JS dan field lain yang bisa menambah order-orderan %>
  <div>
    <%= form.submit %>
  </div>
<% end %>
